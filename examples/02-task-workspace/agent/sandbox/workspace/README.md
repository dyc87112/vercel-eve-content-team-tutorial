# SpringForAll 内容任务工作区

这个目录会在 Eve Sandbox 中映射为 `/workspace`。

## 目录约定

- `knowledge/`：长期资料，例如账号档案、栏目定位、写作规则；
- `inbox/`：用户提交的原始任务和素材；
- `workspace/`：任务执行过程中的阶段性草稿；
- `outputs/`：可以被人类查看、复用或进入下一阶段的成果。

第二章只要求内容负责人读取资料，并把任务分析保存到：

```text
outputs/spring-ai-intro/task-analysis.md
```
