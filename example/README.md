# Examples

这里放每篇教程对应的最小样例工程。

命名规则：

- `01-first-agent/`: 第一个可聊天 Agent。
- `02-custom-provider/`: 自定义 AI Provider、gateway、token plan。
- `03-content-team/`: subagents + skills 内容团队。
- `04-sandbox/`: sandbox 隔离运行产物。

每个样例只展示当前文章需要的最小增量。完整成品放在 `demo/final-content-agent/`。
