# 02 Custom Provider

本目录用于《接入自定义 AI Provider：Gateway、Coding Plan 与 Token Plan》的最小样例工程。

计划展示：

- 默认 Vercel AI Gateway；
- 自定义 OpenAI-Compatible gateway；
- `EVE_MODEL_CONTEXT_WINDOW_TOKENS`；
- provider 检查脚本；
- token plan / context window 说明。

